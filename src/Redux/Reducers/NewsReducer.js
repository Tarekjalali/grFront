

const initialState = {
    news: [], 
    loading: false, 
    error: null,
  };


  const NewsReducer = (state = initialState, action) => {
    switch (action.type) {
    
  
      default:
        return state; 
    }
  };

  export default NewsReducer